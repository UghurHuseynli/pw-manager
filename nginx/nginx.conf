# upstream fastapi_app {
#     server gunicorn-service.pw_manager:8000;
# }

server {
    listen 80;
    server_name localhost;

    client_max_body_size 100M;

    # ALB Health Check Endpoint
    location /api/health {
        access_log off;
        return 200 "OK\n";
    }

    location / {
        proxy_pass http://fastapi_app:8000;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header Host $host;
        proxy_redirect off;

        proxy_connect_timeout 10s;
        proxy_read_timeout 60s;
        proxy_send_timeout 60s;
    }
}


# server {
#     server_name api.medappoint.app;

#     location = /favicon.ico { access_log off; log_not_found off; }

#     location /static/ {
#         root /root/medappoint;
#     }

#     location /media/ {
#             root /root/medappoint;
#         }

#     location / {
#         include proxy_params;
#         proxy_pass http://unix:/run/gunicorn.sock;
#     }

#     listen 443 ssl; # managed by Certbot
#     ssl_certificate /etc/letsencrypt/live/api.medappoint.app/fullchain.pem; # managed by Certbot
#     ssl_certificate_key /etc/letsencrypt/live/api.medappoint.app/privkey.pem; # managed by Certbot
#     include /etc/letsencrypt/options-ssl-nginx.conf; # managed by Certbot
#     ssl_dhparam /etc/letsencrypt/ssl-dhparams.pem; # managed by Certbot

# }
# server {
#     if ($host = api.medappoint.app) {
#         return 301 https://$host$request_uri;
#     } # managed by Certbot


#     listen 80;
#     server_name api.medappoint.app;
#     return 404; # managed by Certbot
