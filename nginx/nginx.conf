# Rate limiting: general API traffic vs. auth endpoints (brute-force guard).
# Zones live in http{} scope — this file is included from the base image's
# http block via /etc/nginx/conf.d/*.conf.
limit_req_zone $binary_remote_addr zone=api_general:10m rate=20r/s;
limit_req_zone $binary_remote_addr zone=api_auth:10m rate=5r/m;
limit_conn_zone $binary_remote_addr zone=conn_addr:10m;

server_tokens off;

gzip on;
gzip_comp_level 5;
gzip_min_length 256;
gzip_proxied any;
gzip_types application/json application/javascript text/css text/plain;

server {
    listen 80;
    server_name localhost;

    client_max_body_size 100M;

    limit_conn conn_addr 20;

    # ALB Health Check Endpoint
    location /api/health {
        access_log off;
        return 200 "OK\n";
    }

    # Brute-force guard on auth endpoints: login, signup, password reset.
    location ~ ^/api/v1/(login/access-token|users/signup|password-recovery/|reset-password/) {
        limit_req zone=api_auth burst=5 nodelay;
        limit_req_status 429;

        proxy_pass http://app:8000;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header Host $host;
        proxy_redirect off;

        proxy_connect_timeout 10s;
        proxy_read_timeout 60s;
        proxy_send_timeout 60s;
    }

    location / {
        limit_req zone=api_general burst=40 nodelay;
        limit_req_status 429;

        proxy_pass http://app:8000;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header Host $host;
        proxy_redirect off;

        proxy_connect_timeout 10s;
        proxy_read_timeout 60s;
        proxy_send_timeout 60s;

        add_header X-Content-Type-Options nosniff always;
        add_header X-Frame-Options DENY always;
        add_header Referrer-Policy same-origin always;
    }
}


# server {
#     server_name api.medappoint.app;

#     location = /favicon.ico { access_log off; log_not_found off; }

#     location /static/ {
#         root /root/medappoint;
#     }

#     location /media/ {
#             root /root/medappoint;
#         }

#     location / {
#         include proxy_params;
#         proxy_pass http://unix:/run/gunicorn.sock;
#     }

#     listen 443 ssl; # managed by Certbot
#     ssl_certificate /etc/letsencrypt/live/api.medappoint.app/fullchain.pem; # managed by Certbot
#     ssl_certificate_key /etc/letsencrypt/live/api.medappoint.app/privkey.pem; # managed by Certbot
#     include /etc/letsencrypt/options-ssl-nginx.conf; # managed by Certbot
#     ssl_dhparam /etc/letsencrypt/ssl-dhparams.pem; # managed by Certbot

# }
# server {
#     if ($host = api.medappoint.app) {
#         return 301 https://$host$request_uri;
#     } # managed by Certbot


#     listen 80;
#     server_name api.medappoint.app;
#     return 404; # managed by Certbot
