services:
  app:
    ports: !override []
    expose:
      - "8000"

  nginx:
    build:
      context: ./nginx
    container_name: pw-manager-nginx
    restart: unless-stopped
    depends_on:
      - app
    ports:
      - "80:80"
      # - "443:443"   # if using SSL / TLS
